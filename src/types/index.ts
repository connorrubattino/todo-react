export type TaskType = {
    title:string,
    description:string,
    dueDate:string,
    completed:boolean
}

export type TaskFormDataType = {
    title:string,
    description:string,
    dueDate:string,
    completed:false
}